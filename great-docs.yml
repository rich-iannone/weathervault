# Great Docs Configuration
# See https://rich-iannone.github.io/great-docs/user-guide/03-configuration.html

# Module Name (optional)
# ----------------------
# Set this if your importable module name differs from the project name.
# Example: project 'py-yaml12' with module name 'yaml12'
# module: yaml12

# Docstring Parser
# ----------------
# The docstring format used in your package (numpy, google, or sphinx)
parser: numpy

# Dynamic Introspection
# ---------------------
# Use runtime introspection for more accurate documentation (default: true)
# Set to false if your package has cyclic alias issues (e.g., PyO3/Rust bindings)
dynamic: false

# API Discovery Settings
# ----------------------
# Exclude items from auto-documentation
# exclude:
#   - InternalClass
#   - helper_function

# Author Information
# ------------------
# Author metadata for display in the landing page sidebar
# You can add additional fields: github, orcid, affiliation, homepage
authors:
  - name: Rich Iannone
    role: Maintainer
    affiliation: Posit, PBC
    email: riannone@me.com
    github: rich-iannone
    orcid: 0000-0003-3925-190X

# API Reference Structure
# -----------------------
# Customize the sections below to organize your API documentation.
# - Reorder items within a section to change their display order
# - Move items between sections or create new sections
# - Use 'members: false' to exclude methods from documentation
# - Add 'desc:' to sections for descriptions

reference:
  title: "API Reference"
  desc: "Complete reference for weathervault functions, objects, and constants."
  sections:
    - title: Core Functions
      desc: Primary functions for retrieving weather data and station information.
      contents:
        - get_weather_data
        - get_station_metadata
        - search_stations

    - title: Station Discovery
      desc: Functions for exploring available stations and their data coverage.
      contents:
        - get_inventory
        - get_years_for_station
        - get_countries

    - title: Helper Objects
      desc: Convenience objects for station and location lookups with autocomplete support.
      contents:
        - name: station
          desc: Helper object for accessing weather station IDs with autocomplete.
        - name: country
          desc: Helper object for accessing ISO 3166-1 alpha-2 country codes with autocomplete.
        - name: state
          desc: Helper object for accessing US state and territory codes with autocomplete.

    - title: Constants
      desc: Data constants and URL configurations used by the package.
      contents:
        - BASE_URL
        - COUNTRY_CODES
        - ISO_COUNTRY_NAMES
        - US_STATE_NAMES

# Site Settings
# -------------
# site:
#   theme: flatly              # Quarto theme (default: flatly)
#   toc: true                  # Show table of contents (default: true)
#   toc-depth: 2               # TOC heading depth (default: 2)
#   toc-title: On this page    # TOC title

# Jupyter Kernel
# --------------
# Jupyter kernel to use for executing code cells in .qmd files.
# This is set at the project level so it applies to all pages, including
# auto-generated API reference pages. Can be overridden in individual .qmd
# file frontmatter if needed for special cases.
jupyter: python3

# CLI Documentation
# -----------------
# cli:
#   enabled: true              # Enable CLI documentation
#   module: my_package.cli     # Module containing Click commands
#   name: cli                  # Name of the Click command object
